function isValid(previous, current, ops) {
    // Compare previous string to current string using the given ops. 
    // if the two strings are the same, return true as the action isValid. 

    
}